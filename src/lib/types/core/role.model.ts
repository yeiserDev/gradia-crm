// src/lib/types/core/role.model.ts

/**
 * Define los roles de usuario que vienen del backend
 * (Basado en la tabla 'Rol' y las constantes)
 */
export type Role = 'ADMIN' | 'DOCENTE' | 'ESTUDIANTE';