export type MessageItem = { id: string; from: string; text: string; time: string; unread?: boolean };
export type NotificationItem = { id: string; title: string; desc?: string; time: string; unread?: boolean; href?: string };
